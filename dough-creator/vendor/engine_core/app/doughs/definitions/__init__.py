"""Dough RECIPES — the DoughStore service (recipe CRUD + glaze) and its
persistence siblings (loading, ids, glaze, cache, box, finder, query). Package
marker only; import from the submodules directly (no re-export barrel)."""
