"""Dough validation cluster: the save/load ref-resolution engine + issue
vocabulary (engine), the independent rule checks (checks), ${ref} drilling
(drill), and the step-shape/id-shape constants + predicates (rules)."""
