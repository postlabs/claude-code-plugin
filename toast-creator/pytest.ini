[pytest]
# First-party script tests only — the vendored trees (vendor/engine_core,
# vendor/peel) are upstream-owned and guarded by their own sync smoke, so they
# are deliberately NOT under test here.
testpaths = tests
addopts = -q
